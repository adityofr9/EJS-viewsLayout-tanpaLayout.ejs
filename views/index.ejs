<%- include ('layout/header') %>
<%- include ('layout/nav') %>
    <br>
    <hr>
    <h3>Contact List</h3>
    <ol>
    <% if (cont.length < 1) { %>
        <p>Contact is empty!</p>
    <% } else { %> 
        <% cont.forEach((contact) => { %>
            <li>Nama: <%= contact.name %> 
                <br>Email: <%= contact.email %></li>
        <% });  %>
    <% } %> 
    </ol>
<%- include ('layout/footer') %> 